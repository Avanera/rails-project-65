// bundle exec rdbg --open --nonstop --command --port=12345 -- bin/rails server -p 3000

{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "ruby_lsp",
      "name": "Attach to Rails server (rdbg)",
      "request": "attach",
      "remoteWorkspaceRoot": "${workspaceFolder}",
      "showProtocolLog": false
    }
  ]
}
